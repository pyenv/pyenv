#!/usr/bin/env bash
# Summary: Show the current Python version
set -e
[ -n "$PYENV_DEBUG" ] && set -x

if [ -z "$PYENV_VERSION" ]; then
  PYENV_VERSION_FILE="$(source "$(command -v pyenv-version-file)")"
  PYENV_VERSION="$(source "$(command -v pyenv-version-file-read)" "$PYENV_VERSION_FILE" || true)"
fi

if [ -z "$PYENV_VERSION" ] || [ "$PYENV_VERSION" = "system" ]; then
  echo "system"
  [ "$0" != "$BASH_SOURCE" ] && return || exit 0
fi

version_exists() {
  local version="$1"
  [ -d "${PYENV_ROOT}/versions/${version}" ]
}

versions=()
OLDIFS="$IFS"
{ IFS=:
  for version in ${PYENV_VERSION}; do
    if version_exists "$version" || [ "$version" = "system" ]; then
      versions=("${versions[@]}" "${version}")
    elif version_exists "${version#python-}"; then
      versions=("${versions[@]}" "${version#python-}")
    else
      echo "pyenv: version \`$version' is not installed" >&2
      [ "$0" != "$BASH_SOURCE" ] && return 1 || exit 1
    fi
  done
}
IFS="$OLDIFS"

OLDIFS="$IFS"
{ IFS=:
  echo "${versions[*]}"
}
IFS="$OLDIFS"
